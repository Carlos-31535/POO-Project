<Window x:Class="POOProject.Views.AddArranjoWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Novo Talão de Arranjo" Height="650" Width="550"
        WindowStartupLocation="CenterScreen">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" BorderBrush="LightGray" BorderThickness="1" Padding="10" CornerRadius="5" Background="#F5F5F5">
            <StackPanel>
                <TextBlock Text="DADOS DO CLIENTE" FontWeight="Bold" Margin="0,0,0,10"/>

                <StackPanel Orientation="Horizontal">
                    <StackPanel Margin="0,0,10,0" Width="220">
                        <Label Content="Nome:"/>
                        <TextBox Text="{Binding NomeCliente, UpdateSourceTrigger=PropertyChanged}" Height="25"/>
                    </StackPanel>

                    <StackPanel Width="220">
                        <Label Content="Sobrenome:"/>
                        <TextBox Text="{Binding SobrenomeCliente, UpdateSourceTrigger=PropertyChanged}" Height="25"/>
                    </StackPanel>
                </StackPanel>

                <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                    <Label Content="Quantos Pares de Sapatos?" VerticalAlignment="Center"/>
                    <ComboBox SelectedValue="{Binding NumeroPares}" SelectedValuePath="Content" Width="60" Margin="10,0,0,0">
                        <ComboBoxItem Content="1" IsSelected="True"/>
                        <ComboBoxItem Content="2"/>
                        <ComboBoxItem Content="3"/>
                        <ComboBoxItem Content="4"/>
                        <ComboBoxItem Content="5"/>
                    </ComboBox>
                </StackPanel>
            </StackPanel>
        </Border>

        <ScrollViewer Grid.Row="1" Margin="0,15,0,0" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding RepairItems}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="#007ACC" BorderThickness="1" CornerRadius="5" Margin="0,0,0,15" Padding="10" Background="White">
                            <StackPanel>
                                <TextBlock Text="{Binding Title}" FontWeight="Bold" Foreground="#007ACC" FontSize="16" Margin="0,0,0,10"/>

                                <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                                    <StackPanel Margin="0,0,20,0" Width="150">
                                        <TextBlock Text="Tipo de Calçado:" FontSize="10" Foreground="Gray"/>
                                        <ComboBox ItemsSource="{Binding TiposDisponiveis}" SelectedItem="{Binding SelectedTipo}"/>
                                    </StackPanel>

                                    <StackPanel Width="150">
                                        <TextBlock Text="Cor:" FontSize="10" Foreground="Gray"/>
                                        <ComboBox ItemsSource="{Binding CoresDisponiveis}" SelectedItem="{Binding SelectedCor}"/>
                                    </StackPanel>
                                </StackPanel>

                                <TextBlock Text="Serviços a realizar:" FontWeight="SemiBold" Margin="0,5,0,5"/>
                                <ItemsControl ItemsSource="{Binding AvailableServices}">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <WrapPanel Orientation="Horizontal"/>
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <CheckBox Content="{Binding Name}" IsChecked="{Binding IsSelected}" Margin="0,0,15,5"/>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>

                                <TextBlock Text="Observações / Defeitos:" Margin="0,10,0,2"/>
                                <TextBox Text="{Binding Description}" Height="40" TextWrapping="Wrap" AcceptsReturn="True" BorderBrush="Gray"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <Button Grid.Row="2" Content="GUARDAR TALÃO" Command="{Binding SaveCommand}" 
                Height="45" Margin="0,10,0,0" Background="#007ACC" Foreground="White" FontWeight="Bold" FontSize="14"/>
    </Grid>
</Window>